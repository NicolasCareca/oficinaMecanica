# app/views/pecas/_form.html.erb

<%= form_with(model: peca, local: true) do |form| %>


  <% if peca.errors.any? %>
    <!-- ... (código de tratamento de erros) ... -->
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :quantidade %>
    <%= form.number_field :quantidade %>
  </div>

  <div class="field">
    <%= form.label :preco %>
    <%= form.number_field :preco %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
