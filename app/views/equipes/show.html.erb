<!-- show.html.erb -->
<div class="container mt-4">
  <div class="mt-3">
    <%= link_to "Voltar", equipes_path, class: "btn btn-danger" %>
  </div>

  <div class="card mt-3">
    <div class="card-header bg-primary text-white">
      Informações da equipe
    </div>
    <div class="card-body">
      <%= render 'equipe_info2', equipe: @equipe %>
    </div>
  </div>
