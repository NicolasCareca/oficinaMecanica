<div class="container mt-4">
  <div class=" col-8 mx-auto">
    <%= link_to "Voltar", equipes_path, class: "btn btn-danger mb-3" %>
  <div class="card ">
  
    <div class="card-body">
      <%= form_with(model: equipe, class: "form") do |form| %>
        <!-- Se houver erros, exiba uma mensagem -->
        <% if equipe.errors.any? %>
          <div class="alert alert-danger">
            <h2><%= pluralize(equipe.errors.count, "error") %> prohibited this veiculo from being saved:</h2>
            <ul>
              <% equipe.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <!-- Campos do formulário -->
        <div class="form-group">
          <%= form.label :nome %>
          <%= form.text_field :nome, class: "form-control", required: true %>
        </div>
        <div class="form-group">
          <%= form.label :descricao %>
          <%= form.text_field :descricao, class: "form-control", required: true %>
        </div>

      
         <div class="form-group mt-3 text-center"> 
          <%= form.submit "Salvar" , class: "btn btn-primary col-sm-12", style: "height: 8vh;" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
</div>
        