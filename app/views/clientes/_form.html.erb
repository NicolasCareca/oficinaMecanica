<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-body">
          <%= form_with(model: cliente, class: "form-horizontal mx-auto") do |form| %>
            <% if cliente.errors.any? %>
              <div class="alert alert-danger">
                <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

                <ul>
                  <% cliente.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="form-group">
              <%= form.label :nome, class: "control-label" %>
              <div class="col-sm-12">
                <%= form.text_field :nome, class: "form-control" %>
              </div>
            </div>

            <div class="form-group">
              <%= form.label :telefone, class: "control-label" %>
              <div class="col-sm-12">
                <%= form.text_field :telefone, class: "form-control" %>
              </div>
            </div>

            <div class="form-group">
              <%= form.label :cpf, class: "control-label" %>
              <div class="col-sm-12">
                <%= form.text_field :cpf, class: "form-control" %>
              </div>
            </div>

            <div class="form-group">
              <%= form.label :rg, class: "control-label" %>
              <div class="col-sm-12">
                <%= form.text_field :rg, class: "form-control" %>
              </div>
            </div>

            <div class="form-group mt-3 text-center"> 
              <%= form.submit class: "btn btn-primary col-6" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
